TODO:
    Make the help option print out useful information or decide if that is even needed.
    Organize loose functions into utils and maybe a standard_board.py
    Test the check functionality to make sure it works
        Add Checking preventing functionality
    Add a way of taking in a file or string and converting it into a board (Allows saving game)
    Make it so that pawns can check the king. Look into this and confirm if even a problem
    In the while loop instead of True have it be a function that returns True only if the current
    player has valid moves they can make.